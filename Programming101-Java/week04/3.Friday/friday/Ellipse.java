package friday;

public final class Ellipse {

	public static void main(String[] args) {
		// TODO Auto-generated method stub

	}

	// constructor with two arguments - numbers, which describe the major axis and the minor axis.
	//if the points are on the origin, print a message "Points are on the origin."
	//constructor that makes a copy of an Ellipse
	//four methods for displaying each point of the bounds of the ellipse. (e.g. the coordinates of an imaginary rectangle that encloses the ellipse)
	//method for displaying the center of the ellipse.
	//methods getPerimeter(), getArea()
	//Override toString(): Ellipse[(x,y), (height,width)]
	//Override equals()
	//Override hashCode()
}
